"""Orchestrator v2 runtime.

This package owns the new task runtime surface. Legacy ``complex_agent`` modules
are compatibility shims only; new MCP/Skill code belongs in
``amadeus_app.orchestrator_integrations`` and new tasks are written to the orchestrator
v2 tables.
"""
